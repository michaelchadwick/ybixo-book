fn greet(name: String) -> String {
    let salutation = if name == "Ybixo" {
        "Greetings"
    } else {
        "Hello"
    }

    "#{salutation}, #{name}!"
}

fn main() {
    let greeting = greet("Ybixo")

    print_line(greeting)
}
